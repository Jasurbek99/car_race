/// SharedPreferences keys used across the app.
class StorageKeys {
  StorageKeys._();

  static const String selectedCarConfig = 'selected_car_config';
  static const String gameProgress = 'game_progress';
}
